#include "sort.h"

/**
 * bitonic_sort - is a parallel algorithm for sorting. It is also used as a
 * construction method for building a sorting network.
 * @array: array to be sorted
 * @size: size of the array
 */

void bitonic_sort(int *array, size_t size)
{
	print_array(array, size);
}
